{
  "name": "jarvis-figma-control",
  "version": "1.0.0",
  "private": true,
  "description": "Figma plugin side of Jarvis voice control (modules/figma_control on the Jarvis backend).",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.94.0",
    "typescript": "^5.4.0"
  }
}
